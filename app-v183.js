/* Bingo Beats V183 — presentatie van de juiste vijf vaste categorieën. */
(function(){
  'use strict';
  function init(){
    document.documentElement.classList.add('bbV183');
  }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',init,{once:true});
  else init();
})();
